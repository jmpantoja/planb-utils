Mejorar las Clases Ensure para hacer cosas asi

PathAssurance::fromString($configFile)
    ->isFile()
    ->haveExtension('yml')
    ->build();

o en versión function

ensure_path($configFile)
    ->isFile()
    ->haveExtension('yml')
    ->build();


StringAssurance::fromString('asdadd')
    ->isEmpty()
    ->isNotBlank()
    ->isMinorThan(5)


(Var Symfony | Zend Validator




